# Testes do módulo Auth

Os testes unitários de autenticação (login, logout, sessão, validators) serão implementados na change `autenticacao-jwt-httponly`.

Convencão: `Auth*Tests.cs` nesta pasta, cobrindo casos de uso da Application com dependências mockadas.
